import QtQuick.Controls

// Only the shared tooltip interface is needed for isolated interaction tests.
// Theme rendering is verified against Omarchy’s real component in the shell.
ToolTip {
    delay: 400
}
